const express = require("express");
const router = express.router();

//this will need to grab the files from the database
Router.get("/", (req, res) => {});


//this will be the route to send chat audio
Router.post("/", (req, res) => {});

module.exports = Router;